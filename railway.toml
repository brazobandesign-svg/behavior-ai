[build]
builder = "nixpacks"

[deploy]
startCommand = "cd exodo-backend && node src/index.js"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
