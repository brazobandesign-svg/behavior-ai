# ── Exodo Web — variables de entorno del cliente ─────────────────────────
# Todo lo que empieza con VITE_ se empaqueta EN EL BUNDLE y es visible por
# F12. Nunca pongas aquí claves privadas (service_role, Stripe secret, etc.).
# Copia este archivo como .env.local para desarrollo.

# URL del backend oficial (Cloud Run). Fallback en código: la URL de producción.
VITE_BACKEND_URL=https://exodo-api-23368377903.us-east1.run.app

# Proyecto Supabase. La anon key es pública por diseño: la protección real
# vive en las políticas RLS de la base (roles anon/authenticated).
VITE_SUPABASE_URL=https://zyvaakfsnlqlgrjdigkr.supabase.co
VITE_SUPABASE_ANON_KEY=eyJ... (pega aquí la anon key del proyecto)
