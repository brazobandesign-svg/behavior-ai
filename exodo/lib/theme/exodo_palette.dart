import 'package:flutter/material.dart';

class ExodoPalette {
  // Fondos oficiales Behavior / Chat (#191919 - #252525 neutral dark / blanco - yeso light)
  static const Color inkDeep   = Color(0xFF191919); // fondo base chat neutro
  static const Color inkRaised = Color(0xFF252525); // tarjetas y modales oscuros
  static const Color inkLine   = Color(0xFF2C2C2E); // bordes neutros
  static const Color paper     = Color(0xFFF5F2EB); // yeso / crema neutro
  static const Color paperSoft = Color(0xFFFFFFFF);

  // Acentos
  static const Color gold      = Color(0xFFC9933A); // acento principal ámbar
  static const Color goldSoft  = Color(0xFFE6B870);
  static const Color goldDeep  = Color(0xFF8A6420);

  // Texto
  static const Color textOnDark  = Color(0xFFF5F2EB);
  static const Color textOnLight = Color(0xFF191919);
  static const Color textMuted   = Color(0xFF8E8E93);

  // Estados
  static const Color success  = Color(0xFF5A8A5A);
  static const Color warning  = Color(0xFFD49B3F);
  static const Color danger   = Color(0xFFE57373);
}
